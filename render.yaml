services:
  - type: web
    name: skill-gap-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: MONGO_URI
        value: mongodb+srv://db_user:N4CUI3hTSaIXIEMA@cluster0.gct52mp.mongodb.net/SkillGapAnalyzer

